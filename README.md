# FlockCameraDetector-M5StickCPlus2
A pocket-sized, passive RF/BLE scanner that flags known Flock Safety ALPR camera and "Raven" hardware signatures — WiFi SSIDs, MAC prefixes, BLE manufacturer IDs, and BLE service UUIDs — with a confidence score, GPS-tagged logging, and on-device screens for stats, live feed, and signal proximity.
